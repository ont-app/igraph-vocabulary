;; shadow-cljs configuration
{:deps {:aliases [shadow]}
 :builds
 {;; Runs shadow node-test
  ;; `shadow-cljs compile lib-test`
  :node-test {:target :node-test
             :output-to "out/test/igraph-vocabulary.js"
             :autorun true
             }
  } ;; /builds
}
